{
  "homepage": "http://stephanietako.github.io/WeatherApp",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "npm run build"
  }
}
